@page
@model MyUploadPage.Pages.Docs.GalleryModel
@{
    ViewData["Title"] = "Gallery";
}

<h1>Gallery</h1>

@foreach (var item in Model.Docs)
{
    <img src="@Url.Action("GetImage","Doc",new {Id=item.Id,FileName=item.FileName,width=300,height=200 })" class="img-fluid" style="max-height:300px"/>
}